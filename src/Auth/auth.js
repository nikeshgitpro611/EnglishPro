// Using JSON Server for authentication instead of Firebase
// This file is no longer needed as we're using json-server with axios

// For future reference - local development setup:
// 1. Run: npm run server (starts json-server on port 5000)
// 2. Use dbApi from utils/dbApi.js for all database operations

export const auth = null;
export const googleProvider = null;
export const db = null;
